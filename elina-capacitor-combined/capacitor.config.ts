import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elina.ai.app',
  appName: 'Elina AI',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
